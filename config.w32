// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("btstore", "for btstore support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("btstore", "enable btstore support", "no");

if (PHP_BTSTORE != "no") {
	EXTENSION("btstore", "btstore.c");
}

